namespace PeasAPI.Enums
{
    public enum UpdateType
    {
        Every,
        OnlyMajor,
        OnlyMinor,
        OnlyBuild,
        MajorAndMinor,
        MajorAndBuild,
        MinorAndBuild
    }
}