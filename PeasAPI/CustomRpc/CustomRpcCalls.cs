namespace PeasAPI.CustomRpc
{
     public enum CustomRpcCalls : uint
    {
        InitializeRoles,
        SetRole,
        ResetRoles,
        CustomEndReason,
        UpdateSetting,
        CheckColor,
        SetColor,
        ShowMessage,
        SetVanillaRole
    }
}